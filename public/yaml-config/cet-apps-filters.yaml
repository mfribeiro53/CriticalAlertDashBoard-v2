# CET Apps - Filter Configuration

filters:
  - column: iGateApp
    type: select
    label: iGate App
    placeholder: Filter by iGate App

  - column: sqlServer
    type: select
    label: SQL Server
    placeholder: Filter by SQL Server

  - column: status
    type: select
    label: Status
    placeholder: Filter by Status
