# CET Dashboard - Filter Configuration

filters:
  - column: iGateApp
    type: select
    label: iGate App
    placeholder: Filter by iGate App

  - column: status
    type: select
    label: Status
    placeholder: Filter by Status
